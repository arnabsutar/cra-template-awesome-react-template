const baseAPI = {
  SAMPLE: {
    apiKey: 'SAMPLE',
    endpoint: `${window.SERVER_DATA.apiHost}/sample`,
    type: 'GET',
    authenticated: true,
    showLoading: true,
  },
};

export default baseAPI;
