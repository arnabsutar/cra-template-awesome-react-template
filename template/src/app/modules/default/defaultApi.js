const defaultAPI = {
  MOD_DEFAULT: {
    apiKey: 'MOD_APP_1',
    endpoint: `${window.SERVER_DATA.apiHost}/MOD_APP_1`,
    type: 'GET',
    authenticated: true,
    showLoading: true,
  },
};

export default defaultAPI;
